import { createContext } from "react";

export const SAMPLE_DATA_KEY = 'SAMPLE_DATA_KEY';
export const ROOT_NODE_ID = 'ROOT_NODE_ID';

export const FormContext = createContext({});
export const StateContext = createContext({});
